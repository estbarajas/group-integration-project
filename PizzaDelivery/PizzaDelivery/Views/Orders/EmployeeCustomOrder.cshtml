@model IEnumerable<PizzaDelivery.Models.Order>

@{
    ViewBag.Title = "EmployeeCustomOrder";
}

<h2>EmployeeCustomOrder</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Total)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderConfirmed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderPrepped)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderOutForDelivery)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Total)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderConfirmed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderPrepped)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderOutForDelivery)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
           
        </td>
    </tr>
}

</table>
